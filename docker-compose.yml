services:
    app:
        build: .
        container_name: poll-story-telegram-bot
        env_file:
            - .env
        volumes:
            - .story_state.yaml:/app/story_state.yaml
            - ./logs:/app/logs
            - .python-cron:/etc/cron.d/python-cron
        restart: unless-stopped
